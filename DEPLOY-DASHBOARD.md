# 🚀 Деплой через Cloudflare Dashboard (НАЙНАДІЙНІШИЙ)

## Проблема з API

Cloudflare Pages API потребує manifest для прямого завантаження, що ускладнює автоматичний деплой через Global API Key.

## Рішення: Dashboard Upload

### Крок 1: Підготовка

```powershell
# Збірка проекту
npm run build
```

### Крок 2: Завантаження через Dashboard

1. Перейдіть: https://dash.cloudflare.com/
2. Виберіть **Pages** → **nexx**
3. Натисніть **"Upload assets"** або **"Deployments"** → **"Create deployment"**
4. Завантажте папку `dist/` або ZIP архів

### Крок 3: Перевірка

```powershell
# Перевірити статус
.\check-deployment.ps1

# Очистити кеш
.\purge-cloudflare-cache.ps1
```

## Альтернатива: Wrangler Login

Якщо хочете автоматичний деплой:

```powershell
# 1. Авторизація через браузер
wrangler login

# 2. Деплой
npm run build
wrangler pages deploy dist --project-name nexx
```

## Поточний статус

- **Останній deployment:** 2026-01-22 16:06:53
- **Production branch:** main
- **Production URL:** https://nexxgsm.com/
- **Проблема:** API деплой не створює deployment (потребує manifest)

## Рекомендація

Використовуйте **Dashboard upload** для надійного деплою або налаштуйте **wrangler login** для автоматизації.
