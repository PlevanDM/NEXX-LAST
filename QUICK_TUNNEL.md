# 🌐 Быстрый запуск туннеля для NEXX

## ✅ Cloudflared найден!

**Расположение:** `C:\Program Files (x86)\cloudflared\cloudflared.exe`

## 🚀 Запуск туннеля

### Вариант 1: Через скрипт (рекомендуется)

```powershell
.\start-tunnel-simple.ps1
```

### Вариант 2: Напрямую

```powershell
& "C:\Program Files (x86)\cloudflared\cloudflared.exe" tunnel --url http://localhost:3000
```

### Вариант 3: Добавить в PATH (для текущей сессии)

```powershell
$env:Path += ";C:\Program Files (x86)\cloudflared"
cloudflared tunnel --url http://localhost:3000
```

## 📋 Что произойдет:

1. Cloudflared создаст публичный URL вида: `https://xxxx-xxxx-xxxx.trycloudflare.com`
2. Этот URL будет перенаправлять на ваш локальный сервер `http://localhost:3000`
3. URL будет показан в консоли

## ⚠️ Важно:

- **Сервер должен быть запущен** на порту 3000 перед запуском туннеля
- Если сервер не запущен, используйте: `npx serve -s dist -l 3000`
- Туннель работает пока открыт терминал (Ctrl+C для остановки)

## 🔄 Альтернативы:

### ngrok:
```powershell
ngrok http 3000
```

### localhost.run (без установки):
```powershell
ssh -R 80:localhost:3000 ssh.localhost.run
```

---

**Готово!** Ваш сайт будет доступен по публичному URL! 🎉
