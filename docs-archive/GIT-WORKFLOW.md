# 🔄 Робочий процес з Git для NEXX WebApp

## 📋 Швидкий старт

Ваш проект підключено до GitHub репозиторію: **https://github.com/PlevanDM/nexx-webapp**

## 🛠️ Доступні команди

### PowerShell скрипти

#### 1. Оновлення з GitHub
```powershell
.\update.ps1
```
Отримує останні зміни з GitHub та застосовує їх до локального проекту.

#### 2. Публікація змін на GitHub
```powershell
.\publish.ps1 -Message "Опис змін"
```
Додає всі зміни, створює коміт та відправляє на GitHub.

**Приклад:**
```powershell
.\publish.ps1 -Message "Додано нову функцію пошуку"
```

#### 3. Повна синхронізація
```powershell
.\sync.ps1
```
Перевіряє локальні зміни, отримує оновлення та показує статус.

### NPM скрипти

```bash
# Перевірка статусу Git
npm run git:status

# Отримання оновлень
npm run git:pull

# Отримання інформації про зміни (без застосування)
npm run git:fetch

# Перегляд останніх комітів
npm run git:log
```

## 📖 Типові сценарії

### Сценарій 1: Отримати оновлення з GitHub

```powershell
# Варіант 1: Використовуючи скрипт
.\update.ps1

# Варіант 2: Вручну
git pull origin main
```

### Сценарій 2: Опублікувати свої зміни

```powershell
# 1. Перевірити статус
git status

# 2. Додати зміни
git add .

# 3. Створити коміт
git commit -m "Опис змін"

# 4. Відправити на GitHub
git push origin main

# Або використати скрипт:
.\publish.ps1 -Message "Опис змін"
```

### Сценарій 3: Перевірити різницю з GitHub

```powershell
# Отримати інформацію про зміни (без застосування)
git fetch origin

# Переглянути різницю
git log HEAD..origin/main --oneline

# Переглянути детальну різницю
git diff HEAD origin/main
```

### Сценарій 4: Відновити файл з GitHub

```powershell
# Відновити конкретний файл
git checkout origin/main -- path/to/file

# Відновити всі зміни до стану GitHub
git reset --hard origin/main
```

## 🔍 Корисні Git команди

### Перегляд історії
```powershell
# Останні 10 комітів
git log --oneline -10

# Детальна історія з графіком
git log --oneline --graph -20

# Історія конкретного файлу
git log --oneline -- path/to/file
```

### Перевірка статусу
```powershell
# Поточний статус
git status

# Короткий статус
git status --short

# Перевірка різниці
git diff
```

### Робота з гілками
```powershell
# Перегляд гілок
git branch -a

# Створення нової гілки
git checkout -b feature/new-feature

# Перемикання на гілку
git checkout main
```

## ⚠️ Важливі примітки

1. **Завжди перевіряйте статус** перед публікацією:
   ```powershell
   git status
   ```

2. **Зберігайте зміни перед оновленням**:
   ```powershell
   git add .
   git commit -m "Збереження змін"
   git pull origin main
   ```

3. **При конфліктах** Git покаже файли з конфліктами. Відредагуйте їх вручну та виконайте:
   ```powershell
   git add .
   git commit -m "Вирішення конфліктів"
   ```

## 🚀 Повний цикл роботи

### Щоденна робота:

1. **Отримати оновлення:**
   ```powershell
   .\update.ps1
   ```

2. **Внести зміни** у код

3. **Перевірити зміни:**
   ```powershell
   git status
   git diff
   ```

4. **Опублікувати:**
   ```powershell
   .\publish.ps1 -Message "Опис ваших змін"
   ```

5. **Збілдити та задеплоїти:**
   ```powershell
   npm run build
   npm run deploy
   ```

## 📞 Допомога

Якщо виникли проблеми:

1. Перевірте підключення до GitHub:
   ```powershell
   git remote -v
   ```

2. Перевірте налаштування Git:
   ```powershell
   git config --list
   ```

3. Перегляньте історію помилок:
   ```powershell
   git reflog
   ```

---

**Репозиторій:** https://github.com/PlevanDM/nexx-webapp  
**Версія:** 9.6.0
