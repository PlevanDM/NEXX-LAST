# 🤖 Налаштування для Cursor AI

## Що налаштовано

### 1. Cursor AI Rules (`.cursor/rules`)
- Структура проекту
- Cloudflare конфігурація
- Команди для деплою
- API endpoints
- i18n налаштування

### 2. Cloudflare Features Check
Скрипт `check-cloudflare-features.ps1` перевіряє:
- ✅ Cloudflare Pages (активний)
- ✅ Cloudflare Workers (доступний)
- ✅ R2 Storage (доступний)
- ✅ KV Storage (доступний)
- ✅ D1 Database (доступний)

## Доступні API

### Поточні дані
- **Global API Key**: `519bdfbd2efeaa9c3a418b905202058bac2fc`
- **Email**: `dmitro.plevan@gmail.com`
- **Account ID**: `ad170d773e79a037e28f4530fd5305a5`
- **Zone**: `nexxgsm.com`
- **Project**: `nexx`

### Що потрібно для повної автоматизації

**API Token** (не Global Key) для:
- ✅ Автоматичного деплою через GitHub Actions
- ✅ Використання wrangler CLI
- ✅ Повного доступу до Pages API

**Як створити:**
1. Перейдіть: https://dash.cloudflare.com/profile/api-tokens
2. Натисніть "Create Token"
3. Використайте шаблон "Edit Cloudflare Workers"
4. Або створіть Custom token з правами:
   - Account → Cloudflare Pages → Edit
   - Account → Workers Scripts → Edit
5. Скопіюйте токен (показується один раз!)

## Автоматизація через Cursor AI

### Доступні команди

```powershell
# Перевірити всі функції Cloudflare
.\check-cloudflare-features.ps1

# Перевірити статус деплою
.\check-deployment.ps1

# Очистити кеш
.\purge-cloudflare-cache.ps1

# Деплой (після налаштування API Token)
.\deploy-wrangler-auto.ps1
```

### GitHub Actions (повністю автоматичний)

Після додавання `CLOUDFLARE_API_TOKEN` в GitHub Secrets:
- Кожен push на `main` автоматично деплоїть
- Не потребує ручного втручання
- Працює з Cursor AI

## Структура для Cursor AI

### Файли конфігурації
- `.cursor/rules` - Правила для Cursor AI
- `wrangler.toml` - Cloudflare конфігурація
- `package.json` - NPM скрипти

### Документація
- `CLOUDFLARE-API.md` - API документація
- `CLOUDFLARE-CONFIG.md` - Налаштування кешування
- `AUTO-DEPLOY-SETUP.md` - Налаштування автоматичного деплою

## Наступні кроки

1. **Створити API Token** (якщо ще не створено)
2. **Додати в GitHub Secrets** (для автоматичного деплою)
3. **Або використати `wrangler login`** (для локального деплою)

Після цього Cursor AI зможе автоматично:
- Деплоїти зміни
- Очищати кеш
- Перевіряти статус
- Керувати всіма функціями Cloudflare
