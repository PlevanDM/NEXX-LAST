# 🚀 Рішення для деплою NEXX WebApp

## Поточна ситуація

- ✅ Проект синхронізовано з GitHub (останні виправлення перекладів)
- ✅ Проект зібрано (production build готовий)
- ⚠️ Потрібна авторизація для деплою

## Проблема з Global API Key

Global API Key (`519bdfbd2efeaa9c3a418b905202058bac2fc`) **не працює** з Wrangler напряму. Wrangler потребує:
- **API Token** (не Global API Key), або
- **OAuth login** через браузер

## Рішення (виберіть один варіант)

### ✅ Варіант 1: OAuth Login (НАЙПРОСТІШИЙ)

```powershell
# 1. Видаліть змінні середовища (якщо встановлені)
Remove-Item Env:CLOUDFLARE_API_TOKEN -ErrorAction SilentlyContinue
Remove-Item Env:CLOUDFLARE_EMAIL -ErrorAction SilentlyContinue

# 2. Авторизуйтеся через браузер
wrangler login

# 3. Задеплоїть
npm run build
wrangler pages deploy dist --project-name nexx
```

**Переваги:**
- Найпростіший спосіб
- Не потрібно створювати токени
- Автоматично налаштовується

### ✅ Варіант 2: Створити API Token

1. **Створіть API Token:**
   - Перейдіть: https://dash.cloudflare.com/profile/api-tokens
   - Натисніть **"Create Token"**
   - Використайте шаблон **"Edit Cloudflare Workers"**
   - Або створіть **Custom token** з правами:
     - **Account** → **Cloudflare Pages** → **Edit**
     - **Account** → **Workers Scripts** → **Edit**

2. **Використайте токен:**
   ```powershell
   $env:CLOUDFLARE_API_TOKEN = "ваш_новий_api_token"
   npm run build
   wrangler pages deploy dist --project-name nexx
   ```

### ✅ Варіант 3: Ручне завантаження через Dashboard

1. **Збілдіть проект:**
   ```powershell
   npm run build
   ```

2. **Завантажте через Dashboard:**
   - Перейдіть: https://dash.cloudflare.com/
   - Виберіть **Pages** → **nexx** проект
   - Натисніть **"Upload assets"**
   - Завантажте папку `dist/`

## Швидкий старт (рекомендовано)

```powershell
# Один раз налаштуйте авторизацію
wrangler login

# Потім завжди просто:
npm run build
wrangler pages deploy dist --project-name nexx
```

## Що буде задеплоєно

Після успішного деплою ваш сайт оновиться з:
- ✅ Виправленнями перекладів (i18n для UK, RO, EN)
- ✅ Оновленим client-v2.js
- ✅ Виправленими кнопками та інтерфейсом
- ✅ Покращеннями PWA

## Перевірка після деплою

1. Відкрийте: https://nexxgsm.com/
2. Перевірте переклади
3. Перевірте консоль браузера на помилки

---

**Рекомендація:** Використайте `wrangler login` - це найпростіший та найнадійніший спосіб!
