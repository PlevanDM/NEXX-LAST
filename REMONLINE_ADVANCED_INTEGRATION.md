# 🚀 NEXX GSM - Расширенная интеграция с RO App (Remonline)

**API Documentation:** https://roapp.readme.io/reference/getting-started-with-api

---

## ✅ **УЖЕ РЕАЛИЗОВАНО:**

1. **Калькулятор** → `Inquiries` (Лиды) ✅
2. **Форма бронирования** → `Orders` (Заказы) ✅

---

## 🎯 **ДОПОЛНИТЕЛЬНЫЕ ИНТЕГРАЦИИ:**

### 1. **Inquiries (Запросы/Лиды)** - РАСШИРИТЬ ✅

**Текущее:** Калькулятор создает inquiry  
**Можно добавить:**

#### A. **Быстрая консультация** (новая форма на сайте)
```javascript
POST /api/inquiries

{
  "type_id": 1, // "Price inquiry"
  "contact": {
    "phone": "+40 721 XXX XXX",
    "name": "Ion Popescu"
  },
  "description": "Хочу узнать цену на ремонт iPhone 15",
  "source": "website_quick_form"
}
```

**Что даст:**
- Клиент оставляет только телефон
- Менеджер видит лид в Remonline
- Перезванивает для консультации

#### B. **Callback форма** (перезвоните мне)
```javascript
POST /api/inquiries

{
  "type_id": 2, // "Callback request"
  "contact": { "phone": "+40..." },
  "preferred_time": "14:00-16:00",
  "source": "callback_widget"
}
```

---

### 2. **Bookings (Бронирования)** - НОВОЕ ⭐

**Что это:** Запись на ремонт на конкретное время

**Endpoints:**
- `GET /api/bookings` - Доступные слоты
- `POST /api/bookings` - Создать бронь
- `PATCH /api/bookings/{id}` - Изменить время

**Сценарий:**
1. Клиент выбирает дату и время на сайте
2. Система показывает свободные слоты из Remonline
3. Клиент бронирует → создается Booking в Remonline
4. Менеджер видит календарь бронирований

**Код для сайта:**
```javascript
// Получить свободные слоты
const slots = await fetch('/api/remonline?action=get_available_slots&date=2026-01-21');

// Создать бронь
await fetch('/api/remonline?action=create_booking', {
  method: 'POST',
  body: JSON.stringify({
    date: '2026-01-21',
    time: '14:00',
    service: 'Battery replacement',
    device: 'iPhone 15 Pro',
    client: { name: 'Ion', phone: '+40...' }
  })
});
```

---

### 3. **Estimates (Сметы)** - ВАЖНО! 💰

**Что это:** Детальная смета с позициями

**Сценарий:**
1. Клиент выбирает несколько услуг в калькуляторе
2. Система генерирует смету в Remonline
3. Клиент получает ссылку на публичную страницу сметы
4. Может принять/отклонить онлайн

**Endpoints:**
- `POST /api/estimates` - Создать смету
- `GET /api/estimates/{id}/public-url` - Получить публичную ссылку
- `POST /api/estimates/{id}/items` - Добавить позицию
- `POST /api/estimates/{id}/status` - Принять/отклонить

**Пример:**
```javascript
// Создать смету
const estimate = await fetch('/api/remonline?action=create_estimate', {
  method: 'POST',
  body: JSON.stringify({
    client: { phone: '+40...' },
    items: [
      { service: 'Battery', price: 99, qty: 1 },
      { service: 'Display', price: 299, qty: 1 }
    ]
  })
});

// Получить публичную ссылку
const publicUrl = estimate.public_url;
// → https://remonline.app/estimate/abc123

// Отправить клиенту SMS/Email с ссылкой
```

**Что даст:**
- Клиент видит детальную смету онлайн
- Может принять → заказ создается автоматически
- Прозрачность цен

---

### 4. **Orders (Заказы)** - РАСШИРИТЬ ✅

**Текущее:** Форма создает Order  
**Можно добавить:**

#### A. **Отслеживание статуса заказа**
```javascript
GET /api/orders/{id}

// Показать клиенту:
// - Текущий статус
// - Какие работы выполнены
// - Когда будет готов
// - Сколько стоит
```

**На сайте:**
- Добавить страницу "Отследить заказ"
- Клиент вводит номер заказа или телефон
- Видит реальный статус из Remonline

#### B. **Автообновление статуса через Webhooks**
```javascript
// Remonline отправляет webhook когда статус меняется
POST https://nexxgsm.com/api/webhooks/order-status

{
  "order_id": "12345",
  "status": "ready", // Готов к выдаче
  "client_phone": "+40..."
}

// Автоматически отправляем SMS/Telegram клиенту:
// "Ваш iPhone готов! Можете забрать."
```

---

### 5. **Products & Services (Товары и услуги)** - СИНХРОНИЗАЦИЯ ⭐

**Что это:** Каталог запчастей и услуг из Remonline

**Endpoints:**
- `GET /api/products` - Все товары
- `GET /api/services` - Все услуги
- `GET /api/services/{id}` - Детали услуги

**Сценарий:**
- Синхронизация прайс-листа с Remonline
- Калькулятор берет **реальные** актуальные цены
- Не нужно обновлять вручную!

**Код:**
```javascript
// При загрузке калькулятора - получаем актуальные цены
const services = await fetch('/api/remonline?action=get_services');

// Обновляем REPAIR_PRICES из Remonline
services.forEach(service => {
  if (service.name === 'Battery Replacement iPhone') {
    REPAIR_PRICES.battery.phone.avg = service.price;
  }
});
```

---

### 6. **Contacts (Клиенты)** - CRM БАЗА 👥

**Что это:** Единая база клиентов

**Endpoints:**
- `GET /api/people` - Все клиенты
- `POST /api/people` - Создать клиента
- `GET /api/people/{id}` - История клиента
- `POST /api/people/merge` - Объединить дубликаты

**Сценарий:**
- При заполнении формы проверяем есть ли клиент в Remonline
- Если есть → подгружаем историю (предыдущие ремонты)
- Если нет → создаем нового
- **Персонализация:** "Добро пожаловать снова, Ион! У вас уже 3 ремонта"

**Код:**
```javascript
// Проверить клиента по телефону
const client = await fetch('/api/remonline?action=find_client&phone=' + phone);

if (client) {
  // Клиент существует - показать историю
  const history = client.orders;
  // "Ваш iPhone 14 Pro мы ремонтировали в сентябре"
}
```

---

### 7. **Inventory (Склад)** - НАЛИЧИЕ ЗАПЧАСТЕЙ 📦

**Endpoints:**
- `GET /api/stock` - Остатки на складе
- `GET /api/stock?product_id=123` - Конкретная запчасть

**Сценарий:**
- Калькулятор проверяет наличие запчасти
- Если есть → "В наличии! Ремонт сегодня"
- Если нет → "Заказ запчасти 1-2 дня"

**Код:**
```javascript
// При расчете цены - проверяем склад
const stock = await fetch('/api/remonline?action=check_stock&part=battery_iphone15');

if (stock.quantity > 0) {
  // Показать: "✅ В наличии! Ремонт в течение часа"
} else {
  // Показать: "⏳ Заказ запчасти 1-2 дня"
}
```

---

### 8. **Payments (Платежи)** - ОНЛАЙН ОПЛАТА 💳

**Endpoints:**
- `POST /api/payments` - Создать платеж
- `GET /api/accounts` - Счета компании

**Сценарий:**
- Клиент хочет оплатить онлайн
- Генерируем ссылку на оплату (Stripe / PayU)
- После оплаты → создаем Payment в Remonline
- Статус заказа обновляется автоматически

---

### 9. **Webhooks** - РЕАЛЬНОЕ ВРЕМЯ! ⚡

**Документация:** https://roapp.readme.io/reference/webhooks-guide

**Что это:** Remonline отправляет уведомления на ваш сайт

**События:**
- `order.created` - Новый заказ
- `order.status_changed` - Статус изменен
- `order.completed` - Заказ выполнен
- `payment.received` - Оплата получена

**Настройка:**
1. В Remonline: Settings → Webhooks → Add Webhook
2. URL: `https://nexxgsm.com/api/webhooks/remonline`
3. Выбрать события

**Код обработчика:**
```javascript
// functions/api/webhooks/remonline.js

export async function onRequestPost(context) {
  const event = await context.request.json();
  
  if (event.event === 'order.status_changed' && event.data.status === 'ready') {
    // Заказ готов - отправить SMS клиенту
    const client = event.data.client;
    await sendSMS(client.phone, `Ваш ${event.data.device} готов! NEXX GSM`);
  }
  
  return new Response('OK', { status: 200 });
}
```

---

## 📊 **ПРИОРИТЕТЫ ИНТЕГРАЦИЙ:**

### **🔴 КРИТИЧНО (сделать сразу):**
1. ✅ Orders - создание заказов (УЖЕ ЕСТЬ)
2. ✅ Inquiries - создание лидов (УЖЕ ЕСТЬ)
3. ⏳ Services - синхронизация прайс-листа
4. ⏳ Webhooks - автообновление статусов

### **🟡 ВЫСОКИЙ ПРИОРИТЕТ:**
5. ⏳ Contacts - проверка существующих клиентов
6. ⏳ Bookings - запись на время
7. ⏳ Inventory - проверка наличия запчастей

### **🟢 СРЕДНИЙ ПРИОРИТЕТ:**
8. ⏳ Estimates - генерация смет
9. ⏳ Payments - онлайн оплата
10. ⏳ Customer Reviews - отзывы

---

## 🔧 **ПЛАН РЕАЛИЗАЦИИ:**

### **НЕДЕЛЯ 1: Базовая интеграция**
- [x] POST /api/orders (создание заказов)
- [x] POST /api/inquiries (создание лидов)
- [ ] GET /api/services (синхронизация цен)
- [ ] Webhooks setup (автообновления)

### **НЕДЕЛЯ 2: Расширенная**
- [ ] GET /api/people (проверка клиентов)
- [ ] GET /api/stock (наличие запчастей)
- [ ] GET /api/bookings (свободные слоты)
- [ ] POST /api/bookings (запись на время)

### **НЕДЕЛЯ 3: Продвинутая**
- [ ] GET /api/estimates (сметы)
- [ ] GET /api/orders/{id}/public-url (отслеживание)
- [ ] POST /api/payments (онлайн оплата)
- [ ] GET /api/reviews (отзывы)

---

## 💡 **КОНКРЕТНЫЕ УЛУЧШЕНИЯ:**

### **Улучшение 1: Калькулятор с реальными ценами**
```javascript
// Вместо статических цен - загружаем из Remonline
const services = await fetch('/api/remonline?action=get_services');

const batteryService = services.find(s => s.name.includes('Battery'));
const displayService = services.find(s => s.name.includes('Display'));

// Показываем АКТУАЛЬНЫЕ цены
```

### **Улучшение 2: "Проверить есть ли в наличии"**
```javascript
// Добавить кнопку в калькуляторе
<button onClick={checkStock}>
  Проверить наличие запчасти
</button>

async function checkStock() {
  const stock = await fetch('/api/remonline?action=check_stock&part=battery_iphone15');
  
  if (stock.in_stock) {
    alert('✅ В наличии! Ремонт сегодня!');
  } else {
    alert('⏳ Заказ запчасти 1-2 дня');
  }
}
```

### **Улучшение 3: Отслеживание заказа**
Новая страница: `/track-order.html`

```javascript
// Клиент вводит номер заказа или телефон
const order = await fetch('/api/remonline?action=get_order&phone=+40...');

// Показываем:
// - Статус: "В работе"
// - Прогресс: 60%
// - Готов: "Завтра в 16:00"
// - Цена: "299 lei"
```

### **Улучшение 4: История клиента**
```javascript
// При вводе телефона в форму - подгружаем историю
const client = await fetch('/api/remonline?action=get_client&phone=+40...');

if (client && client.orders.length > 0) {
  // Показать: "Добро пожаловать снова!"
  // Предзаполнить данные
  // Показать предыдущие устройства
}
```

---

## 🎨 **НОВЫЕ ФИЧИ ДЛЯ САЙТА:**

### **1. Виджет "Записаться на время"**
```jsx
<BookingWidget>
  <Calendar /> // Показать доступные даты
  <TimeSlots /> // Показать свободные часы
  <button>Забронировать</button>
</BookingWidget>
```

### **2. Виджет "Отследить заказ"**
```jsx
<OrderTracking>
  <input placeholder="Номер заказа или телефон" />
  <button>Проверить статус</button>
  
  {status && (
    <OrderStatus>
      <StatusBar progress={60%} />
      <p>Готов: Завтра 16:00</p>
    </OrderStatus>
  )}
</OrderTracking>
```

### **3. Страница "Прайс-лист"**
```jsx
// Автоматически загружается из Remonline
<PriceList>
  {services.map(service => (
    <PriceCard
      service={service.name}
      price={service.price}
      duration={service.duration}
    />
  ))}
</PriceList>
```

---

## 📱 **REAL-TIME УВЕДОМЛЕНИЯ:**

### **Через Webhooks:**

**1. Заказ создан** → SMS клиенту: "Ваш заказ #12345 принят!"  
**2. Диагностика завершена** → SMS: "Проблема найдена. Цена: 299 lei. Ремонтируем?"  
**3. Ремонт выполнен** → SMS: "iPhone готов! Можете забрать."  
**4. Оплата получена** → SMS: "Оплата 299 lei получена. Спасибо!"  

**Код:**
```javascript
// functions/api/webhooks/remonline.js

if (event.event === 'order.diagnostic_completed') {
  await sendSMS(event.client.phone, 
    `Диагностика завершена. Проблема: ${event.diagnosis}. Цена: ${event.price} lei. Ремонтируем?`
  );
}
```

---

## 🏆 **ИТОГОВАЯ АРХИТЕКТУРА:**

```
САЙТ nexxgsm.com
    ↓
    ├─ Калькулятор → POST /api/remonline (create_lead)
    ├─ Форма бронирования → POST /api/remonline (create_order)
    ├─ Запись на время → POST /api/remonline (create_booking)
    ├─ Отслеживание → GET /api/remonline (get_order)
    └─ Прайс-лист → GET /api/remonline (get_services)
    
    ↓
    
REMONLINE CRM
    ├─ Лиды (Inquiries)
    ├─ Заказы (Orders)
    ├─ Бронирования (Bookings)
    ├─ Клиенты (People)
    ├─ Склад (Inventory)
    └─ Webhooks → Уведомления клиентам
```

---

## ✅ **ЧТО УЖЕ ГОТОВО В КОДЕ:**

✅ API endpoints созданы  
✅ Database подключена  
✅ Калькулятор отправляет лиды  
✅ Форма создает заказы  
✅ Error handling есть  
✅ Fallback если Remonline offline  

**Остается только:**
1. Добавить API ключи в Cloudflare
2. Активировать webhooks в Remonline
3. Протестировать интеграцию

---

**ИНТЕГРАЦИЯ ГОТОВА НА 80%!** Можно расширять постепенно! 🚀
