# 🚀 Быстрый запуск NEXX с туннелем

## ✅ Вариант 1: БЕЗ Docker (рекомендуется для Windows)

### Шаг 1: Запустите локальный сервер

```powershell
# Разрешите выполнение скриптов (один раз)
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process -Force

# Соберите проект (если еще не собрали)
npm run build

# Запустите сервер
.\start-server.ps1
# ИЛИ напрямую:
npx serve -s dist -l 3000
```

Сервер будет доступен на: **http://localhost:3000**

### Шаг 2: Настройте туннель для внешнего доступа

#### Вариант A: Cloudflare Tunnel (рекомендуется)

```powershell
# 1. Установите cloudflared
winget install --id Cloudflare.cloudflared

# 2. Авторизуйтесь
cloudflared tunnel login

# 3. Создайте туннель
cloudflared tunnel create nexx-tunnel

# 4. Запустите туннель (в другом терминале)
cloudflared tunnel --url http://localhost:3000
```

**URL будет показан в консоли**, например: `https://xxxx-xxxx-xxxx.trycloudflare.com`

#### Вариант B: ngrok

```powershell
# 1. Установите ngrok
winget install --id ngrok.ngrok
# Или скачайте: https://ngrok.com/download

# 2. Получите токен на https://ngrok.com/
ngrok config add-authtoken YOUR_TOKEN

# 3. Запустите туннель (в другом терминале)
ngrok http 3000
```

**URL будет показан в консоли**, например: `https://xxxx-xxxx-xxxx.ngrok-free.app`

#### Вариант C: localhost.run (без установки)

```powershell
# Установите OpenSSH если нужно
# Затем запустите (в другом терминале):
ssh -R 80:localhost:3000 ssh.localhost.run
```

---

## 🐳 Вариант 2: С Docker

### Если Docker установлен:

```powershell
# Разрешите выполнение скриптов
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process -Force

# Запустите
.\start-docker.ps1
```

### Если Docker НЕ установлен:

Следуйте инструкции из `DOCKER_SETUP.md` или используйте **Вариант 1** (без Docker).

---

## 📋 Быстрая команда (все в одном)

```powershell
# В первом терминале - сервер
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process -Force
npm run build
npx serve -s dist -l 3000

# Во втором терминале - туннель
cloudflared tunnel --url http://localhost:3000
# ИЛИ
ngrok http 3000
```

---

## 🔍 Проверка

1. **Локально**: Откройте http://localhost:3000
2. **Через туннель**: Используйте URL из консоли туннеля

---

## 🆘 Проблемы?

### Порт 3000 занят
```powershell
# Найдите процесс
netstat -ano | findstr ":3000"
# Завершите процесс (замените PID)
taskkill /PID <PID> /F
```

### Скрипт не запускается
```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process -Force
```

### Сервер не отвечает
```powershell
# Пересоберите проект
npm run build
# Проверьте, что dist существует
Test-Path dist
```

---

## 📚 Дополнительная документация

- `DOCKER_SETUP.md` - Настройка Docker
- `TUNNEL_SETUP.md` - Подробная инструкция по туннелям
- `QUICK_START.md` - Краткая справка

---

**Готово!** Ваш сайт доступен локально и через туннель! 🎉
