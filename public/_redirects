# Cloudflare Pages Redirects
# https://developers.cloudflare.com/pages/platform/redirects/

# Главная / — лендинг (index.html)
# / и /index.html отдаются статикой (index.html)

# Личный кабинет — SPA (тот же index.html)
/cabinet           /                 200
/cabinet/          /                 200

# База NEXX — убрана с сайта, все пути → главная
/nexx              /                 301
/nexx/             /                 301
/nexx.html         /                 301
/nexx/*            /                 301
/database          /                 301
/db                /                 301

# Clean URLs for static pages
/about             /about.html       200
/faq               /faq.html         200
/terms             /terms.html       200
/privacy           /privacy.html     200

# Legacy routes
/calculator.html   /calculator       301

# API routes — handled by Cloudflare Pages Functions (functions/api/*)
# Do NOT add redirect rules for /api/* — let Functions handle them

# Static assets
/static/*          /static/:splat    200
/images/*          /images/:splat    200
/data/*            /data/:splat      200

# SPA fallback - only for non-existent routes
