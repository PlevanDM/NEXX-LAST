# Cloudflare Pages Redirects
# https://developers.cloudflare.com/pages/platform/redirects/

# Главная / — лендинг (index.html), база — по /nexx
# / и /index.html отдаются статикой (index.html)

# База NEXX — rewrite на nexx.html
/nexx              /nexx.html        200!
/nexx.html         /nexx.html        200!
/database          /nexx.html        301
/db                /nexx.html        301

# Legacy routes
/calculator.html   /calculator       301
/about.html        /about            301
/faq.html          /faq              301

# API routes (handled by Hono _worker.js)
/api/*             /api/:splat       200

# Static assets
/static/*          /static/:splat    200
/images/*          /images/:splat    200
/data/*            /data/:splat      200

# SPA fallback - only for non-existent routes
# Note: nexx.html and other HTML files should work directly
