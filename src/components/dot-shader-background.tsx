/**
 * Dot shader background — единая реализация.
 * Реэкспорт из landing, чтобы Hero и любой импорт из @/components/dot-shader-background использовали один и тот же рабочий код.
 */
export { DotScreenShader } from '@/landing/components/DotScreenShader'