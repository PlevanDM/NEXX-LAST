# Development версия для запуска Vite dev server
FROM node:20-alpine

WORKDIR /app

# Копируем package файлы
COPY package*.json ./

# Устанавливаем все зависимости (включая dev)
RUN npm ci

# Копируем исходный код
COPY . .

# Открываем порт Vite
EXPOSE 5173

# Запускаем dev server с доступом извне
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
