# 🚀 Інструкції для деплою на Cloudflare Pages

## Проблема

На сайті зараз попередня версія, а на GitHub є нова версія з виправленнями перекладів та іншими оновленнями.

## Рішення

### Варіант 1: Автоматичний деплой (рекомендовано)

```powershell
.\deploy-cloudflare.ps1
```

Скрипт автоматично:
1. Перевірить авторизацію
2. Збере проект
3. Задеплоїть на Cloudflare Pages

### Варіант 2: Через Wrangler CLI

#### Крок 1: Авторизація

```powershell
# Варіант A: Через браузер (найпростіше)
wrangler login

# Варіант B: Через API токен
$env:CLOUDFLARE_API_TOKEN = "ваш_токен_з_правильними_правами"
```

**Як отримати правильний API токен:**
1. Перейдіть на: https://dash.cloudflare.com/profile/api-tokens
2. Натисніть **"Create Token"**
3. Використайте шаблон **"Edit Cloudflare Workers"** або створіть **Custom token**
4. Налаштуйте права:
   - **Account** → **Cloudflare Pages** → **Edit**
   - **Account** → **Workers Scripts** → **Edit**
5. Скопіюйте токен

#### Крок 2: Збірка та деплой

```powershell
# Збірка
npm run build

# Деплой
wrangler pages deploy dist --project-name nexx
```

### Варіант 3: Через Cloudflare Dashboard

1. Перейдіть на: https://dash.cloudflare.com/
2. Виберіть **Pages** → **nexx** проект
3. Натисніть **"Upload assets"**
4. Завантажте папку `dist/`

### Варіант 4: Через GitHub Actions (автоматичний)

Якщо налаштувати GitHub Actions, деплой буде відбуватися автоматично при кожному push на main.

## Перевірка результату

Після деплою:

1. Перевірте сайт: https://nexxgsm.com/
2. Перевірте переклади (оновлені версії)
3. Перевірте консоль браузера на помилки

## Troubleshooting

### Помилка "Unable to authenticate"

- Перевірте, що токен правильний
- Переконайтеся, що токен має права на Pages
- Спробуйте `wrangler login` замість токену

### Помилка "Project not found"

- Перевірте назву проекту: `nexx`
- Створіть проект вручну через Dashboard, якщо потрібно

### Помилка збірки

```powershell
# Очистка та перевстановлення
npm run clean
npm install
npm run build
```

## Останні оновлення на GitHub

Останні коміти містять:
- ✅ Виправлення перекладів (i18n)
- ✅ Оновлення client-v2.js
- ✅ Виправлення кнопок та інтерфейсу
- ✅ Покращення PWA

---

**Після успішного деплою сайт буде оновлено з останніми виправленнями!**
